#include<stdio.h>
int main()
{
    int length;
    int area;
    printf("Enter your length");
    scanf("%d",&length);
  
    
    area= length*length;
    printf("area of rectangle is = %d",area);

}